
# Training + serving dependencies for the SecureNaija ML package.
scikit-learn>=1.4
xgboost>=2.0
pandas>=2.1
numpy>=1.26
scipy>=1.11
joblib>=1.3
pyarrow>=15   # read the IEEE-CIS parquet during Model 2 training
shap>=0.44    # optional at runtime; serving degrades gracefully without it
